# 資金諮詢落地頁

純靜態、手機優先的 FB 廣告落地頁。素材已收錄於 `assets/`。

## 本機預覽

直接開啟 `index.html`，或以任一靜態網站服務器預覽。

## 部署

可上傳此資料夾全部檔案到 Netlify、Vercel、Cloudflare Pages 或既有的靜態網站主機。表單目前為前端成功提示；正式投放前，請將 `script.js` 串接到已授權的 CRM／表單接收端，並補上品牌資料、隱私權政策及必要法規揭露。
